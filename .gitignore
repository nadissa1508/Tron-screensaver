# Ejecutables generados por `make` (el enunciado pide NO entregar ejecutables)
/screensaver.exe
*.o

# Copia de la DLL que el Makefile deja en la raiz al compilar (la version vendorizada
# en libs/SDL2/bin/SDL2.dll SI se versiona, esa es la que hace falta para compilar/correr
# en una maquina limpia)
/SDL2.dll

# Archivos temporales de Word
~$*.docx